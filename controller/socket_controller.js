const socketModel = require('../model/socket');

module.exports.getSocket = async (req,res,next) => {
    try {
        let userID = req.query.userid;
        let getResult = await socketModel.get
    } catch (error) {
        
    }
}